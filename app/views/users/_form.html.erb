<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class= "container text-center">
    
    <div class= "row">
      
      <div class= "col-sm-6 col-sm-offset-3">
        
        <div class= "form-group">
          <%= form.text_field :first_name, id: :user_first_name, class: "form-control user-fn", placeholder: "First Name", required: "true"%>
        </div>

        <div class="form-group">
          <%= form.text_field :last_name, id: :user_last_name, class: "form-control user-fn", placeholder: "Last Name", required: "true" %>
        </div>

        <div class="actions form-group">
          <%= form.submit "Update User", class: "btn btn-info update-btn" %>
        </div>
      </div>
    </div>
  </div>

<% end %>
